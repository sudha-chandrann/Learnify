import { StudyChapter } from '@prisma/client'
import React from 'react'

interface ChapterCardProps {
    chapter:StudyChapter
}

function ChapterCard({chapter}:ChapterCardProps) {
    
  return (
    <div className='rounded'>
chapter
    </div>
  )
}

export default ChapterCard
